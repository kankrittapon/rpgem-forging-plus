# rpgem-forging-plus .gitignore
.gradle/
build/
out/
bin/
.settings/
.project
.classpath
# Local libraries should be kept but JARs often ignored if managed elsewhere,
# but we keep them here as they are part of the mod ecosystem reference.
# libs/*.jar
